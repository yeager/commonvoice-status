#!/usr/bin/env python3
import sys
import os
sys.path.insert(0, '/usr/lib/python3/dist-packages')

try:
    from commonvoice_status.main import main
    main()
except ImportError:
    try:
        from commonvoice_status.app import main
        main()
    except ImportError:
        print("Error: Cannot find main module for commonvoice-status", file=sys.stderr)
        sys.exit(1)
